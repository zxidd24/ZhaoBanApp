# 照办（ZhaoBan）开发手册

> 定位：Android 相册分类整理 App，交互参考 Slidebox —— 全屏单张浏览，靠滑动手势完成「保留 / 删除 / 归类」。
> 目标：先交付一个**最小可运行闭环**（能读相册 → 能滑 → 能分类 → 能删除），再逐步扩展。

---

## 0. 技术选型（MVP）

| 项 | 选择 | 说明 |
|---|---|---|
| 语言 | Kotlin | — |
| UI | Jetpack Compose + Material3 | 手势与分页器实现成本最低 |
| 图片加载 | Coil（`coil-compose`） | 原生支持 ContentResolver URI 与采样缩放 |
| 本地库 | Room | 存分类关系、待删标记、已处理进度 |
| 架构 | 单 Module + MVVM（ViewModel + StateFlow） | MVP 阶段不引入依赖注入 |
| minSdk | 26（Android 8.0） | 覆盖绝大多数机型 |
| targetSdk | **36（Android 16）** | Google Play 自 2026-08-31 起强制要求 ≥ 36 |

> ⚠️ **Android 17（API 37）政策预警**：自 **2026-10-28** 起，target 到 API 37 的应用默认**必须用系统照片选择器（Photo Picker）**，不能再直接申请 `READ_MEDIA_IMAGES` / `READ_MEDIA_VIDEO`；仅「相册管理 / 云存储 / 图片编辑」类应用可申请豁免。
> 照办属于相册管理类，**保留使用 MediaStore 的资格，但需要提前准备豁免申请说明**（见 §7）。MVP 阶段先 target 36，不要提前跳 37。

---

## 1. 第一步：相册读取与修改权限

### 1.1 Manifest 声明

```xml
<!-- 读取：Android 12L 及以下 -->
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
    android:maxSdkVersion="32" />

<!-- 读取：Android 13（API 33）起按媒体类型细分，只申请图片 -->
<uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

<!-- 读取：Android 14（API 34）起支持"部分访问"，用于重新选择照片 -->
<uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

<!-- 可选：读取照片里的地理位置信息 -->
<uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />

<!-- 可选：Android 12+ 免弹窗批量写入（需在系统设置中由用户授权，非运行时弹窗） -->
<uses-permission android:name="android.permission.MANAGE_MEDIA" />
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

<!-- Android 11+ 不再需要 WRITE_EXTERNAL_STORAGE，不要声明 -->
```

### 1.2 运行时申请（按系统版本分流）

```kotlin
fun requiredImagePermissions(): List<String> = when {
    Build.VERSION.SDK_INT >= Build.VERSION_CODES.UPSIDE_DOWN_CAKE -> // 34+
        listOf(Manifest.permission.READ_MEDIA_IMAGES,
               Manifest.permission.READ_MEDIA_VISUAL_USER_SELECTED)
    Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU ->        // 33
        listOf(Manifest.permission.READ_MEDIA_IMAGES)
    else -> listOf(Manifest.permission.READ_EXTERNAL_STORAGE)
}

// Activity 中
private val requestPerms =
    registerForActivityResult(ActivityResultContracts.RequestMultiplePermissions()) { result ->
        val ok = result[Manifest.permission.READ_MEDIA_IMAGES] == true ||
                 result[Manifest.permission.READ_EXTERNAL_STORAGE] == true ||
                 result[Manifest.permission.READ_MEDIA_VISUAL_USER_SELECTED] == true
        if (ok) viewModel.loadPhotos() else showRationale()
    }

// 触发：一次请求全部，避免多次系统弹窗
requestPerms.launch(requiredImagePermissions().toTypedArray())
```

### 1.3 三种访问状态都要处理（Android 14+ 必做）

```kotlin
val full    = checkSelfPermission(READ_MEDIA_IMAGES) == GRANTED
val partial = Build.VERSION.SDK_INT >= UPSIDE_DOWN_CAKE &&
              checkSelfPermission(READ_MEDIA_VISUAL_USER_SELECTED) == GRANTED
// full=false && partial=true  → 只能看到用户勾选的照片
// 两者都 false                → 无权限，显示引导页
```

**硬性要求（踩坑高发区）：**

1. **不要把权限状态持久化**（SharedPreferences / DataStore 都不行）。用户随时可在系统设置里改，`onResume` 必须用 `checkSelfPermission()` 重新判定。
2. **每次 `onResume` 重新查一次 MediaStore**，不要复用缓存列表——部分访问集合会变。
3. **URI 访问视为临时**。用户选的照片可能后续失效，所有 `Uri` 读取都要 try-catch，失败就跳过。
4. 在 UI 上放一个「管理照片权限」按钮，主动触发重新授权，而不是靠系统被动弹窗。

### 1.4 修改权限（写入/删除）—— 不能靠 Manifest

Android 11（API 30）起，修改**其他应用创建**的照片没有静默权限，必须走**用户确认的 PendingIntent**：

| 操作 | API |
|---|---|
| 批量写入（改相册归属 / 移动） | `MediaStore.createWriteRequest(resolver, uris)` |
| 移入系统回收站（推荐，30 天内可恢复） | `MediaStore.createTrashRequest(resolver, uris)` |
| 永久删除（无回收站） | `MediaStore.createDeleteRequest(resolver, uris)` |
| 加入系统「收藏」 | `MediaStore.createFavoriteRequest(resolver, uris)` |

```kotlin
// 启动系统确认弹窗
val pi = MediaStore.createTrashRequest(contentResolver, uris)
startIntentSenderForResult(pi.intentSender, REQ_TRASH, null, 0, 0, 0)
```

若想**跳过每张/每批的确认弹窗**，需引导用户授予 `MANAGE_MEDIA` 特殊权限（系统设置页，不是运行时弹窗）：

```kotlin
startActivity(Intent(Settings.ACTION_REQUEST_MANAGE_MEDIA))
```

> MVP 建议：**不申请 MANAGE_MEDIA**，走系统确认弹窗。流程更简单，也更容易通过 Play 审核。

---

## 2. 第二步：读取照片数据

```kotlin
val collection =
    if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.Q)
        MediaStore.Images.Media.getContentUri(MediaStore.VOLUME_EXTERNAL) // 含 SD 卡
    else
        MediaStore.Images.Media.EXTERNAL_CONTENT_URI

val projection = arrayOf(
    MediaStore.Images.Media._ID,
    MediaStore.Images.Media.DISPLAY_NAME,
    MediaStore.Images.Media.DATE_ADDED,
    MediaStore.Images.Media.BUCKET_DISPLAY_NAME,
    MediaStore.Images.Media.RELATIVE_PATH,   // API 29+
    MediaStore.Images.Media.SIZE,
    MediaStore.Images.Media.MIME_TYPE,
)

contentResolver.query(
    collection, projection, null, null,
    "${MediaStore.Images.Media.DATE_ADDED} DESC"   // 最新优先，符合整理习惯
)?.use { c ->
    while (c.moveToNext()) {
        val id  = c.getLong(c.getColumnIndexOrThrow(MediaStore.Images.Media._ID))
        val uri = ContentUris.withAppendedId(collection, id)
        // ...
    }
}
```

**要点：**

- 查询必须在 **IO 线程**（`Dispatchers.IO`）。
- 照片量大时用 **Paging 3** 分页；MVP 可先一次性加载最新 500 张验证闭环。
- 数据模型：`Photo(id, uri, displayName, dateAdded, bucketName, relativePath, size, mimeType)`。
- 缩略图交给 Coil 处理，不要自己 decode 原图（大图 OOM 是头号崩溃源）。

---

## 3. 第三步：滑动浏览（核心交互）

### 3.1 交互映射（对齐 Slidebox）

| 手势 | 行为 |
|---|---|
| 左右滑动 | 切换上一张 / 下一张，当前照片保持不动 |
| 上滑 | 标记为待删除，自动跳下一张 |
| 下滑 | 加入「收藏」分类 |
| 点击底部相册卡片 | 归入该相册，自动跳下一张 |
| 右下角「+」 | 新建分类相册 |
| 顶部「垃圾桶」 | 进入待删除列表，二次确认后批量清理 |
| 长按 | 查看大图 / 系统信息（MVP 可省略） |

### 3.2 界面骨架

```kotlin
@OptIn(ExperimentalFoundationApi::class)
@Composable
fun ReviewScreen(vm: ReviewViewModel = viewModel()) {
    val photos by vm.photos.collectAsState()
    val pagerState = rememberPagerState(pageCount = { photos.size })
    val scope = rememberCoroutineScope()

    Box(Modifier.fillMaxSize()) {
        // 左右切换：HorizontalPager 原生支持，一次一页
        HorizontalPager(
            state = pagerState,
            beyondViewportPageCount = 1,      // 预加载相邻 1 页，避免白屏
            userScrollEnabled = true
        ) { page ->
            val photo = photos[page]
            AsyncImage(
                model = ImageRequest.Builder(LocalContext.current)
                    .data(photo.uri)
                    .size(Size.ORIGINAL)      // Coil 自动按控件尺寸采样
                    .build(),
                contentDescription = null,
                contentScale = ContentScale.Fit,
                modifier = Modifier.fillMaxSize()
            )
        }

        // 上滑删除 / 下滑收藏：竖直手势层
        VerticalSwipeLayer(
            onSwipeUp   = { vm.markPendingDelete(photos[pagerState.currentPage]); next() },
            onSwipeDown = { vm.addToAlbum(photos[pagerState.currentPage], FAV_ALBUM_ID); next() }
        )

        AlbumDock(                            // 底部相册卡片栏
            albums = vm.albums,
            onPick = { album -> vm.addToAlbum(photos[pagerState.currentPage], album.id); next() },
            onCreate = { name -> vm.createAlbum(name) }
        )
    }
}
```

**要点：**

- `beyondViewportPageCount = 1` 足够，调大反而吃内存。
- Android 16 起**强制边到边**，需 `enableEdgeToEdge()` 并用 `WindowInsets` 处理状态栏/导航栏遮挡。
- 手势层用 `pointerInput` + `detectDragGestures` 判定竖直位移阈值（建议 ≥ 120dp 且竖直位移 > 水平位移），避免与横向 Pager 抢手势。
- 每次操作后 `scope.launch { pagerState.animateScrollToPage(i + 1) }`。

---

## 4. 第四步：基础分类整理

### 4.1 两种实现路径（MVP 选 B）

| 方案 | 做法 | 优点 | 缺点 |
|---|---|---|---|
| **A. 写系统相册** | 改 MediaStore 的 `RELATIVE_PATH`，在系统相册生成真实文件夹 | 与系统相册同步，Slidebox 的做法 | 需 `createWriteRequest` 用户确认；跨版本差异大 |
| **B. 应用内虚拟分类**（推荐 MVP） | Room 记录 `photoId ↔ albumId` 映射，不改系统相册 | 零权限摩擦、实现快、可逆 | 不会同步到系统相册 |

**MVP 走 B，跑通闭环后再补 A。**

### 4.2 方案 B 的数据表

```kotlin
@Entity data class Album(val id: Long = 0, val name: String, val createdAt: Long)
@Entity(primaryKeys = ["photoId", "albumId"])
data class PhotoAlbumCrossRef(val photoId: Long, val albumId: Long)
@Entity data class PendingDelete(val photoId: Long, val markedAt: Long)
```

### 4.3 方案 A 的写入实现（后续扩展）

```kotlin
// 1) 先请求写入授权（一次可传多个 URI）
val pi = MediaStore.createWriteRequest(contentResolver, uris)
startIntentSenderForResult(pi.intentSender, REQ_WRITE, null, 0, 0, 0)

// 2) 授权通过后改 RELATIVE_PATH
val values = ContentValues().apply {
    put(MediaStore.Images.Media.RELATIVE_PATH, "Pictures/照办/$albumName")
}
try {
    contentResolver.update(uri, values, null, null)
} catch (e: RecoverableSecurityException) {   // Android 10/11 常见
    startIntentSenderForResult(e.userAction.actionIntent.intentSender, REQ_WRITE, null, 0, 0, 0)
}
```

> 注意：`RELATIVE_PATH` 仅 API 29+ 可用；API 28 及以下只能走 `DATA` 字段或直接文件操作。

---

## 5. 第五步：删除与回收站

两级设计，降低误删风险（Slidebox 的核心体验）：

1. **上滑** → 写入 `PendingDelete` 表（不触碰系统文件，零弹窗，手感顺滑）。
2. **点垃圾桶 → 确认** → 批量 `MediaStore.createTrashRequest()` 弹一次系统确认，移入系统「最近删除」。

```kotlin
val uris = pendingIds.map { ContentUris.withAppendedId(collection, it) }
val pi = MediaStore.createTrashRequest(contentResolver, uris)
startIntentSenderForResult(pi.intentSender, REQ_TRASH, null, 0, 0, 0)
// onActivityResult 收到 RESULT_OK 后清空 PendingDelete 表
```

> 系统回收站保留期由系统定义（通常 30 天），不要自建倒计时逻辑。

---

## 6. 开发顺序（建议按此推进）

| # | 任务 | 产出 / 验收 |
|---|---|---|
| 1 | 建工程，配好 Compose / Coil / Room | 空壳 App 能跑 |
| 2 | 权限申请 + 三态处理（§1） | 各版本机型能正确拉起授权并识别部分访问 |
| 3 | MediaStore 查询 + Coil 网格页 | 能看到相册全部照片缩略图 |
| 4 | `HorizontalPager` 全屏滑动浏览 | 左右滑动流畅，无白屏 |
| 5 | Room 建表 + 相册 CRUD（方案 B） | 能新建相册、能把照片归类 |
| 6 | 竖直手势层（上滑 / 下滑） | 手势与横向滑动不冲突 |
| 7 | 待删列表 + `createTrashRequest` 批量清理 | 删除后系统相册「最近删除」可见 |
| 8 | 进度持久化（记住"整理到第几张"） | 杀进程重进能续上 |
| 9 | 补方案 A（写系统相册） | 系统相册出现「照办/xx」文件夹 |
| 10 | 上架合规（§7） | 通过 Play 审核 |

---

## 7. 风险与合规清单

| 风险 | 影响 | 应对 |
|---|---|---|
| **Android 17 照片权限政策**（2026-10-28 起） | target 37 后默认禁止直接读全库 | 保持 target 36；准备「相册管理类」豁免材料；同时实现 Photo Picker 作为降级路径 |
| `MANAGE_MEDIA` 审核 | Play 对特殊权限审核严格 | MVP 不申请，用系统确认弹窗 |
| 大图 OOM | 滑动崩溃 | 全程 Coil 采样加载，禁止手动 decode 原图 |
| 部分访问模式下照片突然不可见 | 列表错乱、闪退 | 每次 `onResume` 重查，URI 读取统一 try-catch |
| SD 卡 / 云照片（Google Photos 仅云端） | 部分照片扫不到 | 用 `VOLUME_EXTERNAL`；云端照片明确不支持并在 UI 说明 |
| HEIC / 动图 / 视频 | 格式兼容 | MVP 用 `mimeType` 白名单过滤，只处理 JPEG/PNG/WEBP |
| Android 16 强制边到边 | 全屏页被系统栏遮挡 | `enableEdgeToEdge()` + `WindowInsets` padding |

---

## 8. 参考文档

- [Access media files from shared storage](https://developer.android.com/training/data-storage/shared/media)
- [Partial photo and video access (Android 14)](https://developer.android.com/about/versions/14/changes/partial-photo-video-access)
- [Compose Pager](https://developer.android.com/develop/ui/compose/layouts/pager)
- [Meet Google Play's target API level requirement](https://developer.android.com/google/play/requirements/target-sdk)
